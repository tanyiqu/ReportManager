# ReportManager 产品设计文档

## 1. 产品定位

**ReportManager** 是一款本地优先、跨平台的个人工作记录桌面应用。它用于记录、检索、汇总和导出日报、周报、每日例会记录，替代分散的记事本文件管理方式。

首个发布版本优先支持 Windows 10/11，同时保持 macOS 和主流 Linux 发行版的可构建、可运行能力。

## 2. 产品目标与原则

- 打开应用即可记录，降低日报、周报、会议纪要的编写成本。
- 所有核心功能离线可用；默认不上传任何工作内容。
- 数据保存在程序目录内的本地 SQLite 数据库，并支持用户主动导出备份和整体迁移。
- 保留纯文本记录习惯，同时提供结构化字段和检索能力。
- 界面默认简体中文，采用桌面端左侧导航加主内容区布局。

## 3. 目标用户与典型场景

目标用户是需要定期提交日报、周报并参与每日例会的个人工作者。

1. 每日下班前创建当天日报，逐条记录完成工作、进行中事项和风险。
2. 每日例会时按成员记录任务、状态和截止时间，后续持续跟进。
3. 周末选择本周日报，将内容带入周报草稿后汇总、整理和补充。
4. 按项目、客户、人员、日期或关键词检索过往工作记录。
5. 将单篇记录复制为纯文本或导出 Markdown，发送给主管或归档备份。

## 4. MVP 功能范围

### 4.1 记录类型

| 类型 | 用途 | 默认日期 |
| --- | --- | --- |
| 日报 | 记录当天工作进展、问题与计划 | 当前日期 |
| 周报 | 汇总一周工作、下周计划及例会事项 | 当前周起始日 |
| 每日例会 | 按参会成员记录任务、状态与截止时间 | 当前日期 |

后续可以新增通用会议纪要、月报等类型，但不纳入首版验收范围。

### 4.2 日报

- 支持新建、编辑、保存草稿、保存和删除。
- 自动填写日期；标题默认建议为“YYYY年M月D日日报”，允许修改。
- 支持适合编号列表写法的自由正文编辑。
- 提供可选结构化字段：完成事项、进行中事项、问题与风险、下一步计划、标签。
- 列表展示日期、标题、摘要、标签和最后更新时间。

### 4.3 周报

- 自动计算周起止日期与周次，支持设置周一或周日为每周起始日。
- 提供可编辑分节模板，兼容“上周例会工作完成进度、主要工作、年度管理工作、本周工作总结、下周工作安排、部门学习情况、简要汇报、待办”等常用内容。
- 用户可新增、删除、调整分节。
- 可选择一周范围内的日报，一键带入周报草稿，供人工整理。
- 已手动编辑的周报不得被自动覆盖；再次导入时明确提示追加、替换或取消。

### 4.4 每日例会记录

- 以日期创建例会记录，支持填写会议主题、参会人和会议内容。
- 支持按成员分组录入事项；每个事项至少包含内容、状态、截止时间（可选）和备注。
- 内置状态：未开始、进行中、已完成、已延期；同时允许保留原始文本描述。
- 可从上一日例会复制未完成事项，用户确认后继续跟进。
- 支持以“成员标题 + 事项列表”的纯文本格式复制或导出。

### 4.5 浏览与检索

- 首页展示今日日报、本周周报、今日例会和最近编辑记录。
- 列表可按记录类型、日期范围、标签和关键词筛选。
- 搜索覆盖标题、正文、标签、会议成员和事项内容。
- 支持打开、继续编辑和删除记录。
- 删除前必须二次确认，并提示首版删除后不可恢复。
- 编辑未保存内容时，离开页面提示保存、放弃或继续编辑。

### 4.6 导出与设置

- 将任意单篇记录复制为纯文本到系统剪贴板。
- 将任意单篇记录导出为 Markdown 文件，包含类型、日期、标题、内容和标签；例会额外保留成员与事项状态。
- 设置界面支持语言（首版简体中文）、周报起始日和默认导出目录。
- 可查看数据保存位置并提供“导出全部数据”备份入口。

## 5. 信息架构

| 页面 | 用途 | 核心操作 |
| --- | --- | --- |
| 首页 | 查看今日、本周与最近记录 | 新建今日日报、继续编辑、快速查看 |
| 记录列表 | 浏览与检索全部记录 | 筛选、搜索、打开、删除 |
| 日报编辑器 | 编写和修改日报 | 保存、复制、导出 |
| 周报整理页 | 汇总日报并编辑周报 | 导入日报、整理分节、保存 |
| 例会记录页 | 按成员维护每日例会事项 | 添加成员、更新状态、复制未完成事项 |
| 设置 | 管理偏好与备份 | 周起始日、导出目录、数据导出 |

左侧导航建议包含：首页、日报、周报、例会记录、设置；首页始终提供醒目的“新建今日日报”入口。

## 6. 数据设计（初步）

所有记录使用统一主表，并用 JSON 或关联表保存类型扩展数据；实现时必须通过数据库迁移维护结构演进。

```text
records
id              唯一标识（UUID）
type            daily | weekly | meeting
record_date     日报/例会日期；周报为当周起始日期
title           标题
content         原始正文或结构化内容序列化结果
tags            标签集合
status          draft | saved
created_at      创建时间
updated_at      最后更新时间
```

建议的扩展数据：

```text
日报：completed、in_progress、issues、next_steps
周报：week_end、week_number、sections（分节名称与内容）
例会：meeting_topic、attendees、member_items
事项：member_name、content、item_status、due_date、note、sort_order
```

首版使用本地 SQLite 数据库。Markdown 文件和备份文件用于分享及恢复，不作为唯一数据源。

## 7. 技术方案

- 桌面框架：Tauri 2.x。
- 前端：TypeScript + React + Vite。
- 后端：Rust Tauri Commands。
- 数据库：SQLite，由 Rust 数据访问层统一管理，并使用迁移文件建表和升级；发布版本保存于可执行文件同级的 `data` 目录。
- 平台：优先 Windows 10/11；保持 macOS、Linux 的路径处理和构建兼容。

前端只通过 Tauri Command 调用记录、搜索、导出和设置能力；不得直接访问 SQLite 或拼接 SQL。文件系统访问仅限通过 Rust 端在用户主动选择或设置的目录中进行导出。

### 7.1 便携式数据与窗口行为

- 发布版本的数据库、配置及其他运行时可变文件必须使用相对于可执行文件的 `data` 目录，不得写入系统用户目录；复制整个程序目录即可迁移。
- 开发模式使用 `src-tauri/data`，避免 `clean.bat` 清理 Rust 构建目录时误删开发数据。
- 点击主窗口关闭按钮时应隐藏到系统托盘，不能退出应用进程；仅通过托盘“退出程序”菜单结束应用。
- 单击或双击系统托盘图标、选择“显示主窗口”，以及再次启动可执行程序时，均应恢复已有主窗口而不创建新实例。
- 应用窗口、任务栏和系统托盘图标均由根目录 `logo.png` 生成并通过 Tauri bundle 图标配置使用。
- 主窗口应在前端首帧就绪后再显示，避免出现白屏或空白窗口。

## 8. 非功能需求

- 离线状态下可完整创建、编辑、查询、删除、复制和导出记录。
- 应用重启后，本地已保存数据必须完整可用。
- 所有表单有清晰中文标签、验证/错误提示和键盘可访问性。
- 不将报告内容上传至网络，不要求账号登录。
- 导出、删除等可能影响数据的操作应明确说明结果。

## 9. 首版验收标准

1. Windows 用户可安装启动应用，项目具备 macOS/Linux 构建配置。
2. 用户能创建、编辑、保存、查看和删除日报、周报、每日例会记录。
3. 用户重启应用后仍能看到本地保存的全部记录。
4. 用户能按关键词、日期、类型和标签找到历史记录。
5. 用户能将一周日报导入周报并手动整理，且不会覆盖既有编辑内容。
6. 用户能按成员记录例会事项，并追踪事项状态。
7. 用户能复制或导出任意单篇记录为 Markdown。
8. 以上核心功能在断网环境下可用。

## 10. 暂不纳入首版

- 多人协作、在线共享、审批、账号和云端同步。
- 移动端应用、日历集成、附件上传和富文本排版。
- AI 自动生成日报或周报。
- 复杂统计、提醒通知、任务管理系统集成。

## 11. 开发约定

- 新功能必须优先符合“本地优先、离线可用、简单易用”。
- 开始实现前确认是否属于 MVP；超出范围的能力记录为后续建议。
- 数据结构改动必须提供 SQLite 迁移并保持已有用户数据可用。
- 删除、导出、文件访问需提供清晰的用户提示。
- 记录格式应兼容用户当前的自由文本习惯，结构化字段用于提高整理和检索效率，不强制限制表达方式。

## 12. 开发工具

- 项目根目录的 `clean.bat` 用于清理可重新生成的构建产物与临时缓存，包括 Vite 产物、Rust/Tauri `target` 目录、Tauri schema 缓存和项目日志。
- 清理脚本不得删除 `node_modules`、源码、配置、数据库、导出文件或其他用户数据；依赖清理应由开发者显式执行。
